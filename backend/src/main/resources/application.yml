server:
  port: 8090

spring:
  application:
    name: Q-Entertainment

  datasource:
    url: jdbc:postgresql://localhost:5432/q-entertainment
    username: admin
    password: quang2004

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
    ignore-missing-migrations: true
    out-of-order: true

  jpa:
    database: postgresql
    show-sql: true
